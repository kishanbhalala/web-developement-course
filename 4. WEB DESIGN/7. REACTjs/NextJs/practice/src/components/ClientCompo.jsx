'use client'

import React from 'react'

const ClientCompo = () => {
    return (
        <div>
            <h1>client components</h1>

            <div className='py-5'>
                <p> : Add interactivity and event listeners (onClick(), onChange(), etc)</p>
                <p> : Use State and Lifecycle Effects (useState(), useReducer(), useEffect(), etc)</p>
                <p> : Use browser-only APIs</p>
                <p> : Use custom hooks that depend on state, effects, or browser-only APIs</p>
            </div>

            <p>note: use clinet component maa koi server components no use kro to teee client component bni jaay</p>

        </div>
    )
}

export default ClientCompo
